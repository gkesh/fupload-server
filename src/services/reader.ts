import { Extension } from '../models/file';

const count = (): number => {
    return 0;
}

const listAll = (): string[] => {
    return [];
}

const listByType = (_: Extension): string[] => {
    return [];
}

export {
    count,
    listAll
}